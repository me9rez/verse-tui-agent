{
  "name": "verse",
  "version": "0.0.1",
  "private": true,
  "description": "Verse",
  "type": "module",
  "bin": {
    "verse": "dist/terminal.mjs"
  },
  "scripts": {
    "dev": "node src/cli/terminal.ts",
    "smoke": "node src/checks/smoke.ts",
    "shot": "node src/cli/shot.ts",
    "rpc": "node src/checks/rpc-check.ts",
    "complete": "node src/checks/complete-check.ts",
    "build": "tsdown",
    "backend": "cd backend && uv run python rpc_server.py",
    "config-test": "cd backend && uv run python test_config.py",
    "typecheck": "tsc -p tsconfig.json",
    "sessions": "node src/checks/session-check.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@simon_he/vue-tui": "^1.1.11",
    "vue": "^3.5.43"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsdown": "^0.23.0",
    "typescript": "^7.0.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
